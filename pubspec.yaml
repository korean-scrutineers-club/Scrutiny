name: scrutiny_workspace
publish_to: none

environment:
  sdk: ^3.13.1

workspace:
  - app
  #  - admin
  - packages/core
  - packages/domain
  - packages/data

dev_dependencies:
  flutter_lints: ^6.0.0
